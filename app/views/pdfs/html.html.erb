<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= @pdf.name %> - HTML Content</title>
  <style>
    body {
      margin: 0;
      padding: 20px;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
    }
    .header {
      background: #f5f5f5;
      padding: 15px 20px;
      margin: -20px -20px 20px -20px;
      border-bottom: 1px solid #ddd;
    }
    .header h1 {
      margin: 0 0 10px 0;
      font-size: 24px;
    }
    .back-link {
      color: #0066cc;
      text-decoration: none;
    }
    .back-link:hover {
      text-decoration: underline;
    }
    .content {
      max-width: 1200px;
      margin: 0 auto;
    }
    .no-content {
      padding: 40px;
      text-align: center;
      color: #666;
      background: #f9f9f9;
      border-radius: 8px;
    }
  </style>
</head>
<body>
  <div class="header">
    <div class="content">
      <h1><%= @pdf.name %></h1>
      <%= link_to "← Back to PDF", game_pdf_path(@game, @pdf), class: "back-link" %>
    </div>
  </div>

  <div class="content">
    <% if @pdf.html_content.present? %>
      <iframe srcdoc="<%= @pdf.html_content.gsub('"', '&quot;') %>"
              style="width: 100%; min-height: 800px; border: 1px solid #ddd; background: white;"
              onload="this.style.height = (this.contentWindow.document.documentElement.scrollHeight + 20) + 'px';">
      </iframe>
    <% else %>
      <div class="no-content">
        <p>No HTML content available for this PDF.</p>
        <p>The PDF may still be processing or the HTML extraction may have failed.</p>
      </div>
    <% end %>
  </div>
</body>
</html>
